#!/bin/bash
# scrotplus - the simple screengrabber

dir="$HOME/.scrots" # change this to change where you want the scrots to be saved to
[ -d $dir ] && : || mkdir $dir

cd $dir
ffcast -s png "$(date '+%F_%H:%M:%S').png"
xclip -selection clipboard -t image/png -i "$(command ls -1 | sed -n '$p')"
